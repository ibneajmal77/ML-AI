services:
  api:
    ports:
      - "8002:8000"
      - "5678:5678"
    volumes:
      - ./src:/app/src
    command: >
      python -m debugpy
      --listen 0.0.0.0:5678
      --wait-for-client
      -m uvicorn src.api.main:app
      --host 0.0.0.0
      --port 8000
      --reload
